name: Test Workflow
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Node.js
        uses: v-jitenderpalsingh/setup-node@test-1556
        with:
          node-version: '18.18.1'

      - name: Run Main
        run: |
          node main.js
          node -v
          
      